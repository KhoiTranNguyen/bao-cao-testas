{
  "name": "bao-cao-testas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && esbuild index.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --jsx=automatic && cp index.html dist/index.html"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0"
  }
}
